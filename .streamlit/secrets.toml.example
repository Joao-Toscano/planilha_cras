# Copie este arquivo para .streamlit/secrets.toml (uso local) OU cole o
# conteúdo nas Secrets do seu app no Streamlit Community Cloud
# (Settings do app > Secrets). NUNCA suba o secrets.toml de verdade para o
# GitHub — ele contém a senha do seu banco de dados.

supabase_db_url = "postgresql://postgres.SEU_PROJETO:SUA_SENHA@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
